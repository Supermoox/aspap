<div class="row ">
  <div class="col-md custom_wrapper">      
    <h3 id="form-header">Edit Membership</h3>
    <div class="my-form">
    <%= form_with(model: @membership, local: true) do |form| %>
      <% if @membership.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@membership.errors.count, "error") %> prohibited this membership from being saved:</h2>

          <ul>
          <% membership.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

        <div class="form-group">
          <p id="form-lebel"><%= form.label :Membership_Type %><p/>
          <%=  form.collection_select :kind, Membership.kinds.map{ |a| [a.first,a.first] },  :first, :second , :required => true %>
        </div>
        <div class="form-group">
          <p id="form-lebel"><%= form.label :Upload_Member_ID %></p>
          <%= form.file_field :card, :required => true %>
        </div>
        <div class="actions">
          <%= form.submit %>
        </div>
    <% end %>
    </div>
  </div>
</div>
 




