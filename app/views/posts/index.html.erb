<div class="main-content">
  <div class="journal-header">
    <p>Discussions</p>
  </div>
  <div class="panel">
    <% @posts.each do |post| %> 
      <%= link_to post do %> 
        <div class="row clickable-div">
          <div class="col-sm-3 ">
            <% if post.user.image.blank? %>
              <p id="user_avatar2"><img src="<%= asset_path( 'default_user.png' ) %>" /></p>
            <% else %>
              <p id="user_avatar2"><%= image_tag post.user.image %></p>
            <% end %>
          </div>       
          <div class="col-sm-9 ">
          <div class="discuss">
            <p ><strong style="font-family: initial;"><%=  post.title  %> </strong></p>
            <div class="fadded">
              <span><%=  post.body  %> </span>
            </div>
          </div>
          </div>  
        </div>
      <% end %>
    <% end %>
    <p> <%= will_paginate @posts %> </p>
    <div class="post-form">
      <%= form_for  @post do |f| %>
        <div class="form-group">
          <p id="form-lebel"><%= f.label :Title %><p>
          <p> <%= f.text_field :title, :placeholder =>"title...", class: "form-control"%></p>
        </div>

        <div class="form-group">
          <p id="post-text"><%= f.text_area :body, :placeholder =>"write here...", class: "form-control" %></p>
        </div>

        <div class="actions">
          <%= f.submit %>
        </div>
      <% end %>
    </div>
  </div>
</div>

<%= link_to root_path do %>
  <div class="example">Hey!</div>
<% end %>




